# ravejs

Install development dependencies:
```bash
yarn install
```

Run the development server:
```bash
yarn run dev
```

Build library:
```bash
yarn run build
```

Preview build:
```bash
yarn run preview
```
